// handoff v2 · Screens · from design/ (2af582ff5ba8) — generated by the server; never edit, never regenerate

export const Screens = {
  /** 이력서 — 유영훈 — design/Resume.dc.html */
  resume: "resume",
  /** Forest — 명상 사운드 믹서 — design/Forest.dc.html */
  forest: "forest",
  /** Handoff Agent — 핸드오프 플러그인 — design/Handoff Agent.dc.html */
  handoffAgent: "handoffAgent",
} as const;

export type ScreenId = (typeof Screens)[keyof typeof Screens];

/** URL path per screen — the human-confirmed route (design/handoff.manifest.json → screens[].path). */
export const ScreenPaths: Record<ScreenId, string> = {
  resume: "/resume",
  forest: "/forest",
  handoffAgent: "/handoffAgent",
};
