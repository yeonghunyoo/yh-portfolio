// handoff v2 · DesignTokens · from design/ (2af582ff5ba8) — generated by the server; never edit, never regenerate

/** Dimensions are px numbers; colors are strings. In CSS use the same tokens as var(--key-with-dashes) from tokens.css. */
export const DesignTokens = {
  Accent: {
    /** accent.ink = #0F1115 — CSS: var(--accent-ink) */
    ink: "#0F1115",
    /** accent.soft = rgba(61,220,151,.12) — CSS: var(--accent-soft) */
    soft: "rgba(61,220,151,.12)",
  },
  Code: {
    /** code.bg = #0A0C10 — CSS: var(--code-bg) */
    bg: "#0A0C10",
    /** code.dim = #6F7783 — CSS: var(--code-dim) */
    dim: "#6F7783",
    /** code.ink = #C9CED6 — CSS: var(--code-ink) */
    ink: "#C9CED6",
  },
  Line: {
    /** line.soft = rgba(230,232,236,.08) — CSS: var(--line-soft) */
    soft: "rgba(230,232,236,.08)",
  },
  /** accent = #3DDC97 — CSS: var(--accent) */
  accent: "#3DDC97",
  /** bg = #0F1115 — CSS: var(--bg) */
  bg: "#0F1115",
  /** card = #141821 — CSS: var(--card) */
  card: "#141821",
  /** hl = rgba(61,220,151,.14) — CSS: var(--hl) */
  hl: "rgba(61,220,151,.14)",
  /** ink = #E6E8EC — CSS: var(--ink) */
  ink: "#E6E8EC",
  /** line = rgba(230,232,236,.16) — CSS: var(--line) */
  line: "rgba(230,232,236,.16)",
  /** muted = #8B93A1 — CSS: var(--muted) */
  muted: "#8B93A1",
  /** surface = #181C24 — CSS: var(--surface) */
  surface: "#181C24",
} as const;
